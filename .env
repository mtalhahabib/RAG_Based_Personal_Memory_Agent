# 🧭 Paths to watch (comma-separated)
# Example: WATCH_PATHS=E:\CodeAcm, E:\Projects, C:\Users\<user>\Documents
WATCH_PATHS=E:\CodeAcm,E:\CodeAcm\CodeAcme Repos

# 📦 Git repository watching
# Auto-discover option: set to 'true' to find all git repos under watch paths
GIT_AUTO_DISCOVER=true
# Optional: explicit git repos to always watch (comma-separated), even if outside watch paths
GIT_WATCH_PATHS=E:\CodeAcm\

# 🚫 Excluded folders or patterns (comma-separated)
# These will be skipped by watcher and indexer.
EXCLUDE_PATTERNS=.git,node_modules,__pycache__,.venv,.idea,.db,.db-journal

# 🤖 LLM backend (choose one)
# gemini  → Uses local Gemini CLI
# ollama  → Uses Ollama server + local model
# stub    → Offline no-LLM fallback mode
LLM_BACKEND=gemini

# 🧠 Local vector memory DB (used by indexer/chat)
VECTOR_DB=memory_vectors.db

# 📋 Local event database (used by watcher and indexer)
EVENT_DB=events.db

# ⚙️ Optional: delay for event batching (used by watcher)
DEBOUNCE_SEC=0.4

# 💬 Model configuration (optional overrides)
GEMINI_MODEL=gemini-2.5-flash
OLLAMA_MODEL=llama3

# 🔑 Authentication (optional for Gemini CLI)
# Only needed if Gemini CLI hasn’t been configured interactively.
# Uncomment and add your key if required.
GEMINI_API_KEY=AIzaSyA_vEguqVCOxZaED1KGSHayCUuOZ0py-CE
# 🧩 Full path to Gemini CLI (for Windows)
GEMINI_CMD=C:\Users\Pc planet\AppData\Roaming\npm\gemini.cmd


